# V7.0.0 升级说明与交付清单

> 本文档对应需求文档第十八节的全部交付项。本次升级是在 V6.5.2 基础上的**增量开发**：
> 原有评分管线、数据通道、账户与会话逻辑一行未删；新增内容全部位于独立的
> “历史相似情景决策层”（决策引擎版本 `V7.0.0`），可随时整体移除而不影响原系统。

---

## 1. 修改后的完整项目

项目位置不变：`Agent_A_正式版_V6.5.2`（代码基线 `MODEL_VERSION="V6.5.2"`，
该常量被回归测试锁定，故保留；新增决策层以 `DECISION_ENGINE_VERSION="V7.0.0"` 标记）。

修改前的完整原始备份已保存在工作目录
`backup_Agent_A_V6.5.2_original`，可随时对比或回滚。

## 2. 新增／修改文件清单

| 文件 | 状态 | 角色 |
| --- | --- | --- |
| `lookahead_guard.py` | 新增 | 时间边界与防前视守卫（最高优先级保护） |
| `historical_context.py` | 新增 | 7 维度情景登记与历史可得特征构造 |
| `historical_similarity.py` | 新增 | 相似度数学模型与案例检索 |
| `historical_outcome.py` | 新增 | 历史案例后续 1/5/20/60/120 日表现统计 |
| `evidence_quality.py` | 新增 | Similarity × Reliability × DataQuality 证据分级 |
| `event_reaction.py` | 新增 | 事件→预期→实际价格反应→量能判定 |
| `technical_patterns.py` | 新增 | 红三兵有效性、趋势位置、RSI/MACD/均线/动量 |
| `dynamic_weight.py` | 新增 | A/B/C 三档动态权重（总和恒=100%） |
| `historical_decision.py` | 新增 | 决策编排 + 交叉验证降权 + 六段式解释 |
| `test_v7.py` | 新增 | 12 类共 21 项新测试 |
| `app.py` | 修改 | 新增“历史情景决策”页签与两处调用（约 +180 行，无删改） |
| `README.md` | 修改 | 增加 V7.0.0 章节与文件表 |
| `版本更新清单_V7.0.0.md` | 新增 | 简要更新记录 |
| 其余全部文件 | 未改动 | `agent_core.py`、`factor_analysis.py`、`news_analysis.py`、`questionnaire.py`、`cloud_store.py`、`session_memory.py`、`snapshot_codec.py`、`add_position_analysis.py`、全部旧测试、SQL 建表脚本均保持原样 |

## 3. 每个文件修改／新增了什么

- **lookahead_guard.py**：`TimeBoundary` 时间边界；`truncate_to_boundary` 截断；
  `ensure_selection_dates_within_boundary`、`ensure_outcome_observed_after_selection`、
  `ensure_feature_window_ends_at_or_before` 三类守卫；违反时抛 `LookaheadViolation`。
- **historical_context.py**：7 个情景组登记（股票自身状态、市场整体环境、行业、宏观、
  政策、事件、预期），历史不可得组写明原因；构造 28 个历史可得特征
  （多期限收益、波动、下行波动、回撤、MA20/60/120 相对位置、量比、量能 z 值、RSI14、MACD 柱）。
- **historical_similarity.py**：IQR 稳健标准化 → 加权欧氏距离 → 指数衰减相似度；
  组权重（股票状态 0.65／市场环境 0.35）仅在可得组之间归一；
  候选区间 = [预热期 130 日, T−120 日]，与当前至少间隔 20 日；
  案例间距 ≥20 日；严格阈值 62 分，不足 6 个时放宽到 52 分并降低证据质量。
- **historical_outcome.py**：选中后才读取结果；各期限平均/中位收益、上涨频率、
  10/25/75 分位、最大涨幅、最大回撤、最大亏损、峰值时间、路径形态
  （先跌后涨/先涨后跌/单边/震荡）、量能变化、突破前高比例；跨期限方向强度。
- **evidence_quality.py**：三层证据公式与 HIGH/MEDIUM/LOW 阈值；
  历史不可得维度计入数据质量扣分而不是被忽略。
- **event_reaction.py**：把每条重要资讯映射到发布后第一个交易日，
  读取当日跳空、收盘反应、冲高回落、量能与事件前 5 日漂移，
  归类为：正向确认／可能利好兑现／利好已被提前计价／利好钝化／风险确认／利空已被提前计价等；
  有实际反应数据时“市场反应主导、情绪减半”。
- **technical_patterns.py**：趋势位置分类（底部/上涨早段/中段/顶部/下跌/震荡）；
  红三兵识别与 8 项有效性检查（前期趋势、位置、实体、上影线、量能、突破、底部背景、新闻吻合），
  归类为趋势反转/超跌反弹/消息刺激/主力试盘/诱多/上涨末端/延续；
  综合 RSI14、MACD、波动调整动量输出 0—100 方向分。
- **dynamic_weight.py**：A 档 50%—65%、B 档 20%—50%、C 档 0%—20%；
  模块不可用时权重按比例转移；四舍五入残差回补最大权重，保证严格求和=100%。
- **historical_decision.py**：按“当前状态→相似情景→历史表现→资讯反应→技术形态→
  原有因子→动态权重→最终判断”的顺序编排；基本面 ≤35 分一票降权 25%、
  ≤45 分降权 10%、≥2 项经营恶化降权 15%；核心卖出信号把综合分压到 45 以下；
  输出综合评分、建议、风险等级、置信度、建议仓位与六段式“为什么”解释。
- **app.py**：导入 `build_v7_decision`；完整分析流程在资讯评估后计算
  `analysis["historical_decision"]`（失败自动降级，不影响原结论）；
  结果页与历史快照页都新增“历史情景决策”页签；旧快照无该字段时按快照内行情重算。
- **test_v7.py**：21 项测试覆盖需求第十六节全部 12 类（详见第 8 节）。

## 4. 核心数学模型公式

### 4.1 特征标准化（组内每个特征）

```
scale_i = IQR_i(候选集合)          # 退化时退回 std_i，再退化为 1
z_{t,i} = clip( (x_{t,i} - x_{T,i}) / scale_i, -8, +8 )
```

### 4.2 组相似度与总相似度

```
d_g = sqrt( Σ_i w_{g,i} · z_{t,i}² )        # 组内加权欧氏距离，Σw=1
Sim_g = 100 · exp(-0.40 · d_g)
Historical Similarity = Σ_g ( W_g · Sim_g ) / Σ_g W_g
    W = {股票自身状态: 0.65, 市场整体环境: 0.35}，仅在可得组之间归一
```

缺失组不进入分子也不进入分母——缺失数据不会抬高相似度（防人为制造高相似）。

### 4.3 案例筛选

```
候选区间: 130 ≤ pos(t) ≤ rows(T) - 1 - 120 且 pos(t) ≤ rows(T) - 1 - 20
选择: 相似度降序、案例间距 ≥ 20 个交易日
阈值: 严格 62 分；不足 6 个样本时放宽 52 分并下调证据质量
```

### 4.4 历史证据分

```
Historical Evidence Score = Similarity × Reliability × DataQuality × 100 / 100

Reliability = 0.35·min(n/15,1)
            + 0.25·(1 - IQR_20日结果 / (2·0.04·√20))
            + 0.20·跨期限方向一致比例
            + 0.20·(1 - std(相似度)/15)

DataQuality = 0.35·特征覆盖率 + 0.25·维度组覆盖率
            + 0.25·min(历史行数/750,1) + 0.15·行情新鲜度

分级: HIGH ≥45 且 n≥8 且平均相似度≥62；MEDIUM ≥25 且 n≥6；其余 LOW
```

### 4.5 动态权重（A/B/C）

```
HIGH:   w_hist = 0.50 + 0.15·clip((E-45)/55,0,1)      # 50%—65%，且必须最大
MEDIUM: w_hist = 0.20 + 0.30·clip((E-25)/25,0,1)      # 20%—50%
LOW:    w_hist = 0.20·clip(E/25,0,1)                  # 0%—20%，禁止≥50%
剩余权重按 38%/34%/28% 分配给 资讯事件/行情技术/原有因子，不可用模块份额按比例转移
约束: Σweights = 100%（程序断言，违反即拒绝输出）
```

### 4.6 模块方向分（均为 0—100，50 为中性）

```
历史模块:   50 + 35·方向强度·(0.5+0.5·Reliability)
方向强度 = Σ_h ω_h · [ 0.5·clip((胜率_h-0.5)·2) + 0.5·clip(中位收益_h/(0.02·√h)) ]
           ω = {5日:0.15, 20日:0.30, 60日:0.30, 120日:0.15}（1日仅展示）
资讯模块:   50 + 0.5·情绪分·18 + 1.5·事件反应均分（有反应数据时市场反应主导）
技术模块:   50 + 趋势位置分 + 红三兵贡献 + RSI/MACD/动量修正
原有因子:   直接采用 V6.5.2 所选周期时点评分（不重算）
综合分 = Σ w_k · Score_k × 交叉验证降权系数(1 / 0.9 / 0.85 / 0.75)
```

### 4.7 建议映射

```
综合分 ≥65 → 建议买入；55—65 → 谨慎买入；45—55 → 观望；<45 → 不建议买入
个人适配=不适配 → 最高只给“观望”；核心卖出信号触发 → 综合分上限 45
```

## 5. Agent 整体架构

```
                 ┌──────────────────────────────────────────────┐
                 │            V6.5.2 原有核心（完全保留）          │
                 │  数据通道 → 量化指标 → 6期限评分 → 适配/仓位/卖出 │
                 └──────────────┬───────────────────────────────┘
                                │ 原有输出作为“原有因子交叉验证”模块
                                ▼
┌─────────────────────────  V7.0.0 决策层（新增） ─────────────────────────┐
│                                                                          │
│  行情(截断到T) ─→ historical_context(7维度) ─→ historical_similarity      │
│                        │                            │ 相似度+候选案例      │
│                        │                            ▼                     │
│                        │                 historical_outcome(先选后看)      │
│                        │                            │ 1/5/20/60/120日表现  │
│                        ▼                            ▼                     │
│                 evidence_quality ◄──────────────────┘                     │
│                 (HIGH/MEDIUM/LOW)                                         │
│                        │                                                  │
│  资讯 ─→ event_reaction        行情 ─→ technical_patterns                 │
│  (事件→预期→价格反应→量能)      (红三兵/趋势位置/RSI/MACD)                  │
│                        │                                                  │
│                 dynamic_weight (A:≥50% / B:20-50% / C:0-20%)              │
│                        │                                                  │
│                 historical_decision (加权 + 交叉验证降权 + 六段式解释)      │
└──────────────────────────────────────────────────────────────────────────┘
  全程受 lookahead_guard 约束：T 之后的数据在任何环节都不可见。
```

## 6. Tool 清单

| 函数（工具） | 所在模块 | 输入 | 输出 |
| --- | --- | --- | --- |
| `build_boundary` / `truncate_to_boundary` | lookahead_guard | 日期序列、分析位置 | 时间边界、截断数据 |
| `build_context_frame` | historical_context | 个股+基准行情（≤T） | 28 特征表、组登记、缺失维度说明 |
| `search_historical_cases` | historical_similarity | 行情、配置、分析位置 | 相似度序列、选中案例、边界 |
| `evaluate_outcomes` | historical_outcome | close/volume、已选案例 | 各期限统计与路径形态 |
| `assess_evidence` | evidence_quality | 检索与结果统计 | Evidence 分、等级、构成明细 |
| `assess_event_reaction` | event_reaction | 资讯结果、行情 | 事件反应判定、资讯模块分 |
| `assess_technical_patterns` | technical_patterns | 行情、新闻日期 | 红三兵、趋势位置、技术模块分 |
| `determine_weights` | dynamic_weight | 证据等级/分、模块可用性 | 权重（和=100%）与理由 |
| `build_v7_decision` | historical_decision | bundle、原分析、资讯 | 综合决策 + 解释（JSON 可存快照） |
| 原有：`fetch_price_bundle`、`analyze_all`、`assess_news`、`analyze_sell_signals`、`build_factor_analysis` | 原模块 | 不变 | 不变 |

## 7. 数据需求清单

| 数据 | 用途 | 当前来源 | 历史可得性 |
| --- | --- | --- | --- |
| 个股日线（开高低收量，近5年） | 股票自身状态特征、技术形态、结果观察 | Yahoo图表/备援通道（V6.5.2 既有） | ✅ 全部历史 |
| 市场基准日线（沪深300/SPY/HSI） | 市场环境维度、相对强弱 | V6.5.2 既有 | ✅ 全部历史 |
| 最新财务/估值 | 原有因子交叉验证、一票降权 | BaoStock/SEC/东方财富/Yahoo | ⚠️ 仅最新披露 |
| 最新宏观快照（LPR/美债） | 当前侧宏观描述 | AKShare/FRED | ⚠️ 仅最新值 |
| 近 7—30 天资讯 | 事件—市场反应 | Google/Yahoo/Bing RSS（V6.3 既有） | ⚠️ 仅近期 |
| 行业指数与行业估值历史 | 行业环境维度 | ❌ 无稳定免费通道 | 标记“数据暂不可用” |
| 宏观历史时点快照 | 宏观环境维度 | ❌ 免费通道无点态数据 | 标记“数据暂不可用” |
| 历史政策库 | 政策环境维度 | ❌ 无 | 标记“数据暂不可用” |
| 历史新闻事件库 | 事件性质维度 | ❌ 无 | 标记“数据暂不可用” |
| 分析师一致预期历史 | 市场预期维度 | ❌ 无 | 用“事件前漂移”近似提示，不伪造数值 |

## 8. 测试结果

**PASS**。环境：Python 3.12.10、pandas 3.0.5、numpy 2.5.2、pytest 9.1.1，全部离线。

| 套件 | 结果 |
| --- | --- |
| 原有测试 test_v5 / test_v6 / test_v62 / test_v63 / test_v64 / test_v65 / test_v652 | **46/46 通过**（与改动前基线一致，0 失败） |
| 新增测试 test_v7.py | **21/21 通过** |
| 合计 | **67/67 通过，0 失败** |
| Streamlit 页面 | `AppTest` 脚本级运行 0 异常；健康检查 ok |

新增 21 项对需求 12 类的覆盖：

1. 相似度计算：重复片段高相似（≥75）、远态低相似（2 项）；
2. 案例筛选：间距≥20 日、按相似度降序、阈值、结果窗口完整（1 项）；
3. 可信度：小样本/离散惩罚、缺失维度只降数据质量不抬相似度（2 项）;
4. 高质量权重≥50%且最大（1 项）；
5. 低质量权重≤20%（含 LOW+任意证据分扫描）（1 项）；
6. 权重总和=100%：3 等级 × 21 个证据分 × 4 种可用性组合全扫描（1 项）；
7. 新闻市场反应：正向确认/提前计价/利好钝化（2 项）；
8. 红三兵：形态识别、有效性≥55、无形态不误报、分数边界（2 项）；
9. 缺失数据：无资讯/无财务/短历史/无基准全组合不崩溃、权重仍=100%（1 项）；
10. 异常数据：0 量、NaN 量、单日 ×3 异常不崩溃、短序列明确拒绝（1 项）；
11. 未来数据泄露：篡改 T 之后价格/量能/基准，选择结果逐位不变；边界越界抛错；结果窗口可观察性（3 项）；
12. 回归：原管线输出逐字节不变、快照往返、页面接线、基本面一票降权（5 项）。

失败项：**无**；对原有功能影响：**无**（原有 46 项在改动前后结果完全一致）。

## 9. 未来数据泄露防护说明

1. **结构隔离**：`search_historical_cases` 第一步就把行情截断到分析日 T，
   之后所有特征、标准化参数（IQR 取自候选集）、相似度、案例选择都只依赖 ≤T 的行；
2. **候选约束**：候选案例位置 ≤ T−120（最长结果窗口），保证“先选案例、后看结果”；
   且与 T 至少间隔 20 个交易日，避免把当前状态自身选成历史案例；
3. **顺序约束**：`evaluate_outcomes` 只接收已选案例列表，不参与也不回头影响选择；
4. **财务口径**：沿用 V6.5.2 的 `_latest_public_financial_row`（只取不晚于分析日的披露行）；
5. **显式守卫**：`ensure_selection_dates_within_boundary` 等函数在选择完成后复核，
   任何使用未来日期的行为都会抛出 `LookaheadViolation`；
6. **测试证明**：`test_v7_15` 将 T 之后的收盘价乘 1→4 倍、成交量放大 99 倍、
   基准改为 0.2 倍，案例选择与相似度逐位不变。

## 10. 当前仍然存在的数据缺口

1. **行业维度全缺**：无稳定免费的行业指数/估值/资金流历史，行业环境只能人工补充；
2. **宏观历史时点快照缺失**：LPR、利率、流动性只有最新值，宏观环境无法进入历史相似度；
3. **政策与事件库缺失**：历史政策方向、重大事件分类无法程序化还原；
4. **一致预期缺失**：分析师预期、业绩预期无历史数据，只能用事件前价格漂移做近似提示；
5. **换手率/筹码缺失**：免费通道无流通股本，量能用成交量比与 z 值近似；
6. **资讯窗口短**：只有近 7—30 天新闻，更早的事件反应无法回看；
7. **日线时点限制**：事件当日只有日线近似（开盘跳空/冲高回落），无分钟数据；
8. **单市场基准近似**：港股以恒指、美股以 SPY 为基准，行业结构偏差无法消除。

以上缺口在报告中全部显式标注为“数据暂不可用”，并通过数据质量分压低历史证据等级，
不伪造、不回填、不用今天的数据冒充历史。

## 11. 下一阶段建议

1. **接入行业指数**（如申万/中证行业指数的免费历史），把行业环境升级为历史可得维度；
2. **宏观点态库**：用 AKShare/FRED 的月度序列自建“发布日对齐”的宏观快照表，
   使宏观维度进入历史相似度（注意发布滞后，必须按发布日而非统计期对齐）；
3. **历史新闻/公告库**：把每日抓取的资讯落库（现有 Supabase 可扩展），
   滚动 6—12 个月后即可支持事件性质与预期的历史匹配；
4. **多股票案例池**：当前是“同股历史”，下一步可在同行业股票间建立案例池，
   并用 `walk_forward` 证据决定跨股票样本的折扣；
5. **权重再校准**：用 `factor_analysis` 的滚动验证框架对 Evidence 阈值（45/25）
   与档位区间做多股票样本外复核后再微调；
6. **分钟级事件反应**：接入分钟数据后，把事件反应从“当日近似”升级为“盘中真实反应”；
7. **预期差量化**：接入业绩预告/快报日历后，可用“预告 vs 市场前期走势”量化预期差；
8. **决策层 LLM 解释器**：`historical_decision` 的输出为纯结构化数据，
   可直接交给外层 LLM 生成自然语言解读（数学计算仍全部由程序完成）。

---

## 附：纪律声明

- 未重新制作 Agent，未推倒重写，未删除任何原有功能；
- 未因数据缺失而编造数据或人为抬高相似度；
- 未使用任何未来信息参与案例选择；
- 所有核心数学计算由程序完成；解释文本由计算结果模板化生成；
- 结果仅供研究参考，不构成投资建议。
