# 请先看这里

把 `Agent_A_正式版_V6.5.2` 文件夹**里面的全部内容**上传到Agent A的GitHub仓库根目录。

上传成功后，网页标题必须显示：

`个人投资者股票决策辅助 Agent｜数据渠道稳定版 V6.5.2`

如果没有显示V6.5.2，说明上传位置不对，网页仍在运行旧文件。

Main file path：`app.py`

不需要重新运行SQL，不需要修改Secrets。Agent B本次不修改。

