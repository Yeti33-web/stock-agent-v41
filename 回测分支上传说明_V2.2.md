# 回测App分支部署说明

## 一、先建立分支

1. 打开Agent A的GitHub仓库。
2. 确认左上角当前分支是 `main`。
3. 点击分支名称 `main`。
4. 在输入框填写 `historical-backtest`。
5. 点击 `Create branch: historical-backtest from main`。

这个新分支会先完整复制当前Agent A V6.5.2，因此根目录中仍然有 `app.py`、`agent_core.py` 和其他正式模型文件。

## 二、上传回测工具

1. 确认GitHub左上角当前显示 `historical-backtest`，不要停留在 `main`。
2. 解压回测分支覆盖包。
3. 打开 `Agent_B_回测分支覆盖包_V2.2`。
4. 把里面的 `historical_test_tool` 整个文件夹上传到仓库根目录。
5. 提交后确认能看到 `historical_test_tool/app.py`。

不要删除分支根目录原有的 `app.py`、`agent_core.py`、`factor_analysis.py`、`questionnaire.py` 和 `requirements.txt`。回测工具需要读取这些最新的Agent A文件。

## 三、在Streamlit新建第二个App

打开：https://share.streamlit.io/

点击 `Create app` 或 `New app`，选择：

- Repository：与Agent A相同的GitHub仓库
- Branch：`historical-backtest`
- Main file path：`historical_test_tool/app.py`

然后点击 `Deploy`。

正式Agent A原来的App保持：

- Branch：`main`
- Main file path：`app.py`

不要修改正式Agent A现有App的分支和Main file path。

## 四、以后Agent A升级后

GitHub分支不会自动同步。每次Agent A升级后，需要把新版A的核心文件也覆盖到 `historical-backtest` 分支，至少包括：

- `app.py`
- `agent_core.py`
- `factor_analysis.py`
- `questionnaire.py`
- `news_analysis.py`
- `requirements.txt`

回测工具本身不根据未来行情改权重，只是读取该分支中的最新Agent A规则，并把数据截断到历史日期T。

