# 回测App分支部署说明（V2.3，同步 Agent A V7.0.0）

## 一、先建立分支

1. 打开Agent A的GitHub仓库。
2. 确认左上角当前分支是 `main`。
3. 点击分支名称 `main`。
4. 在输入框填写 `historical-backtest`。
5. 点击 `Create branch: historical-backtest from main`。

这个新分支会先完整复制当前Agent A（V6.5.2核心 + V7.0.0历史情景决策层），因此根目录中仍然有 `app.py`、`agent_core.py`、`historical_decision.py` 和其他正式模型文件。

## 二、上传回测工具

1. 确认GitHub左上角当前显示 `historical-backtest`，不要停留在 `main`。
2. 解压回测分支覆盖包。
3. 打开 `Agent_B_回测分支覆盖包`。
4. 把里面的 `historical_test_tool` 整个文件夹上传到仓库根目录。
5. 提交后确认能看到 `historical_test_tool/app.py`。

不要删除分支根目录原有的 `app.py`、`agent_core.py`、`historical_decision.py`、`factor_analysis.py`、`questionnaire.py` 和 `requirements.txt`。回测工具需要读取这些最新的Agent A文件。

## 三、在Streamlit新建第二个App

打开：https://share.streamlit.io/

点击 `Create app` 或 `New app`，选择：

- Repository：与Agent A相同的GitHub仓库
- Branch：`historical-backtest`
- Main file path：`historical_test_tool/app.py`

然后点击 `Deploy`。

正式Agent A原来的App保持：

- Branch：`main`
- Main file path：`app.py`

不要修改正式Agent A现有App的分支和Main file path。

## 四、以后Agent A升级后（重要）

GitHub分支不会自动同步。每次Agent A升级后，需要把新版A的核心文件也覆盖到 `historical-backtest` 分支。

最小同步清单（V7.0.0 起）：

- `app.py`
- `agent_core.py`
- `factor_analysis.py`
- `questionnaire.py`
- `news_analysis.py`
- `requirements.txt`

只要A包含V7.0.0决策层，还必须同步以下全部文件，否则回测分支的“历史情景决策”页签会停留在旧版：

- `historical_decision.py`
- `historical_context.py`
- `historical_similarity.py`
- `historical_outcome.py`
- `evidence_quality.py`
- `event_reaction.py`
- `technical_patterns.py`
- `dynamic_weight.py`
- `lookahead_guard.py`

可选增强：历史资讯档案 `news_archive.db`（放在分支根目录）。在本机用
`python -m historical_test_tool.news_archive --code 600519 --start 2019-01-01`
建仓后提交到分支；回测只读取其中发布时间不晚于T的条目。注意东方财富公开检索约只保留最近6个月文章，更老的T自动按“资讯不参与”处理。抓取只在本机跑，不要在Streamlit Cloud上跑。

更稳妥的做法：每次A升级后，直接把A的全部文件覆盖到 `historical-backtest` 分支根目录（`historical_test_tool` 文件夹本身不受影响）。

回测工具本身不根据未来行情改权重，只是读取该分支中的最新Agent A规则，并把数据截断到历史日期T。

## 五、版本对应关系

- Agent A 正式版：`main` 分支，Streamlit 入口 `app.py`。
- Agent B 回测版：`historical-backtest` 分支，Streamlit 入口 `historical_test_tool/app.py`。
- 当前回测工具版本：V2.3，与 Agent A V7.0.0（核心 V6.5.2 + 历史情景决策层）同步；含可选历史资讯档案（A股·东方财富）。
